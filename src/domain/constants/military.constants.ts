/**
 * Constantes relacionadas a militares e RG
 */

/**
 * RG mínimo válido (inclusivo)
 */
export const MIN_RG_VALUE = 1;

/**
 * RG máximo válido (inclusivo)
 */
export const MAX_RG_VALUE = 10000;

/**
 * RG reservado para usuário de sistema/seed
 * Este RG é filtrado nas consultas públicas por questões de segurança
 */
export const SYSTEM_USER_RG = 9999;
